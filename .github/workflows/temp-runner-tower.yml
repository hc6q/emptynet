name: TEMP runner tower patch
on:
  push:
    branches: [fix/runner-tower-primary-scene]
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/runner-tower-primary-scene
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Patch
        run: echo pending